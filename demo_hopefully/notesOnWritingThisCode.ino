//demo_hopefully will be my main file... the only one with a setup or a loop
//so all loops need to get moved into here
//we set flags for each step to figure out what code is running in the loop when
//start by functionitizing existing loops

//after commenting the code so it's all organized under the same headings and whatnot
//need to condense setup of colorSensor, hitMole, lineFollowDistanceSensor, and moveMole
//same for loop

//but figure out how to have them all coexist
//first of this will be to functionitize the loops
//do i also functionitize the setups?? probably
//but overlap so like...... we will see

//ORDER OF EVENTS
//1. Calibrate, signal somehow, then wait for start button to be pressed
//2. lineFollowDistanceSensor until distance is detected
//3. coinSlot until done
//4. pressRedButton, set at white, move on when done
//5. whack! by looping through until the moles start dancing (aka readings go beserk? or just after 2 minutes?)
//  A. colorSensor
//  B. moveMole
//  C. lineFollowDistanceSensor
//  D. hitMole


//You can call a function declared not in main
//You can declare and initialize variables in main and call them in other files
//so lets do it!!

//formatting:
  //lineFollowDistanceSensor------------------------------------------------------------------------------------------------------------
  
  //coinSlot------------------------------------------------------------------------------------------------------------
  
  //pressRedButton------------------------------------------------------------------------------------------------------------

  //colorSensor------------------------------------------------------------------------------------------------------------
  
  //moveMole------------------------------------------------------------------------------------------------------------
  
  //hitMole------------------------------------------------------------------------------------------------------------ 

  //MAIN (demo_hopefully)------------------------------------------------------------------------------------------------------------
